import React from 'react';
import { Typography } from '@mui/material';

const Testimonials: React.FC = () => {
    return (
        <div>
            <Typography variant="h4">Testimonials Page</Typography>
            <Typography variant="body1">Welcome to the Testimonials Page.</Typography>
        </div>
    );
};

export default Testimonials;
